id: pg-readonly
version: "1"
type: postgres
description: Read-only PostgreSQL database access
permissions:
  scope:
    - read:tables
conn:
  url_env: "PG_RO_URL"
limits:
  rate_per_min: 120
  timeout_s: 20
