id: fetch
version: "1"
type: mcp
description: Web content fetching and conversion for efficient LLM usage
transport:
  type: stdio
  command: npx
  args:
    - -y
    - "@pulsemcp/pulse-fetch@0.3.0"
permissions:
  scope:
    - read:web
limits:
  rate_per_min: 30
  timeout_s: 45
