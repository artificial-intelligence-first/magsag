version: 0.1.0
id: skill.obsidian-note-append
name: ObsidianNoteAppend
description: Append content to an Obsidian vault note via the MCP stdio transport.

entrypoint: "@magsag/catalog#skills.obsidianNoteAppend"

permissions:
  - mcp:obsidian

resource_config:
  estimated_duration: short
  timeout_ms: 15000
  cost_category: low

observability:
  logs: true
  metrics: true
  traces: basic
