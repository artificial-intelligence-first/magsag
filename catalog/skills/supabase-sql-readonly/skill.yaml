version: 0.1.0
id: skill.supabase-sql-readonly
name: SupabaseSqlReadonly
description: Execute read-only SQL queries against the Supabase MCP endpoint.

entrypoint: packages/catalog-mcp/src/tools/placeholders.ts:createSupabaseSqlReadonlyPlaceholderTool

permissions:
  - mcp:supabase

resource_config:
  estimated_duration: short
  timeout_ms: 25000
  cost_category: low

observability:
  logs: true
  metrics: true
  traces: basic
