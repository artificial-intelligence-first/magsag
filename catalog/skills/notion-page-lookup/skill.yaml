version: 0.1.0
id: skill.notion-page-lookup
name: NotionPageLookup
description: Retrieve Notion pages through the standard HTTP-first MCP transport.

entrypoint: catalog/skills/notion-page-lookup/impl/notion_page.py:run

permissions:
  - mcp:notion

resource_config:
  estimated_duration: medium
  timeout_ms: 20000
  cost_category: low

observability:
  logs: true
  metrics: true
  traces: basic
