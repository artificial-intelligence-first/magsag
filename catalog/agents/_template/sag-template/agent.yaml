slug: "your-advisor-sag"
name: "YourAdvisorSAG"
role: "sub"
version: "0.1.0"
description: "Sub-agent for [describe specialized task]"

entrypoint: "@magsag/catalog#agents.yourAdvisorSag"

depends_on:
  skills:
    - "skill.your-domain-skill"

contracts:
  input_schema: "catalog/contracts/your_advisor_input.schema.json"
  output_schema: "catalog/contracts/your_advisor_output.schema.json"

risk_class: "low"

budgets:
  tokens: 50000
  time_s: 10

provider_config:
  task_type: "computation"
  estimated_duration: "short"
  resource_hint: "standard"
  provider_hint: "local"

observability:
  logs: true
  metrics: true
  traces: "basic"

evaluation:
  success_criteria:
    - "output conforms to schema"
  performance_targets:
    p95_latency_ms: 2000
    success_rate: 0.98
