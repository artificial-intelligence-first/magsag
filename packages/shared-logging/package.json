{
  "name": "@magsag/shared-logging",
  "version": "0.1.0",
  "description": "Shared logging utilities for the TypeScript monorepo.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest --run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pino": "^9.2.0"
  },
  "engines": {
    "node": ">=18.18"
  }
}
