{
  "name": "@magsag/demo-api",
  "version": "2.0.0-alpha.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest --run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@magsag/demo-shared": "workspace:*"
  }
}
