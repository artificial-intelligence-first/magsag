{
  "name": "magsag-monorepo",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest --run",
    "docs:lint": "tsx ops/tools/check-docs.ts",
    "catalog:validate": "tsx ops/tools/validate-catalog.ts",
    "verify:vendor": "tsx ops/tools/verify-vendor.ts",
    "test:watch": "vitest",
    "test:unit": "vitest --run --project unit",
    "test:integration": "vitest --run --project integration",
    "test:cli": "vitest --run --project cli",
    "test:e2e": "vitest --run --config vitest.config.ts tests/vitest/e2e/*.test.ts",
    "ci:lint": "pnpm -r lint",
    "ci:typecheck": "pnpm -r typecheck",
    "ci:build": "pnpm -r build",
    "ci:test": "vitest --run --config vitest.config.ts",
    "ci:e2e": "vitest --run --config vitest.config.ts tests/vitest/e2e/*.test.ts",
    "ci:size": "node ops/scripts/check_package_size.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "@types/split2": "^4.2.3",
    "eslint": "^8.57.0",
    "eslint-plugin-vitest": "^0.5.4",
    "tsup": "^8.1.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.18.0",
    "vitest": "^1.6.1",
    "tsx": "^4.15.7",
    "yaml": "^2.6.0"
  }
}
